#!/usr/bin/env bash

case "$1" in
	"log" ) bash ~/opt/pslog/bin/log ;;
	"cat" ) bash ~/opt/pslog/bin/cat ;;
	"clear" ) rm ~/opt/pslog/data/*; echo "pslog history cleared." ;;
	* ) echo "Set an argument: log, cat, or clear" ;;
esac
