#!/usr/bin/env bash

# cd ~/opt/pslog/
# add process log
cd $(dirname $0) && cd ..
logfile=~/.local/share/pslog/$(date +%s)
osascript src/fg.* | sd ', ' '\n' >> $logfile &&
echo -e "\n---\n" >> $logfile &&
osascript src/bg.* | sd ', ' '\n' >> $logfile
